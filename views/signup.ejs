<!DOCTYPE html>
<html lang="en">
<head>
    <style>
        body {
            font-family: 'Franklin Gothic Medium', 'Arial Narrow', Arial, sans-serif;
        }
        label {
            display: block;
            margin-top: 10px;
        }
        input {
            width: 100%;
            padding: 8px;
            margin-top: 5px;
            margin-bottom: 10px;
        }
        button {
            padding: 10px 15px;
            background-color: #007BFF;
            color: white;
            border: none;
            cursor: pointer;
        }
        button:hover {
            background-color: #0056b3;
        }
    </style>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Signup Form</title>
</head>
<body>
    <h1>Signup</h1>
    <form action="/user" method="post">
        <label for="name">Enter Full Name</label>
        <input type="text" id="name" required name="name" placeholder="e.g., Manan Fayaz">
        
        <label for="email">Enter Email</label>
        <input type="email" id="email" required name="email" placeholder="e.g., johndoe@gmail.com">
        
        <label for="password">Enter Password</label>
        <input type="password" id="password" required name="password" placeholder="e.g., YourSecurePassword123">
        
        <button type="submit">Signup</button>
    </form>
</body>
</html>
